<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Allow access to USB devices (required for thermal printer) -->
    <key>com.apple.security.device.usb</key>
    <true/>
    <!-- Allow JIT compilation (required by Electron/Chromium) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required by Electron) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dyld environment variables (required by Electron) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
    <!-- Disable library validation (required for native node modules) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Network client (required to load the Vercel web app) -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
